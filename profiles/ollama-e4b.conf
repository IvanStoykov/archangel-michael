# Ollama — fast default for pipeline validation
PROFILE_NAME=ollama-e4b
AUDIT_MODE=bundled
HERMES_EXTRA_ARGS="-Q --max-turns 5"
HERMES_PROVIDER=ollama-launch
HERMES_MODEL=gemma4:e4b
HERMES_BASE_URL=http://127.0.0.1:11434/v1
API_HEALTH_URL=http://127.0.0.1:11434/v1/models
# Ollama reports model context; Hermes may need 65536+ — smoke_test will warn
# Ollama reports 131072 for gemma4:e4b; Hermes requires >= 65536
HERMES_CONTEXT_LENGTH=131072
BACKEND_PORT=11434
BACKEND_START_CMD=""
BACKEND_STOP_PATTERN=""
